; BIND reverse data file for local loopback interface
;
$TTL    604800
@ IN SOA ns.99h.info. root.ns.99h.info. (
                              2         ; Serial
                             1w         ; Refresh
                             1d         ; Retry
                             1m         ; Expire
                             1w )       ; Negative Cache TTL
;
@      IN      NS      ns.
5      IN      PTR     ns.99h.info.
10     IN      PTR     www.99h.info.
